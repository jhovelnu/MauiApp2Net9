<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiApp2Net9.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Maui;assembly=FFImageLoading.Maui"
    xmlns:local="clr-namespace:MauiApp2Net9"
    HideSoftInputOnTapped="True">

    <ContentPage.BindingContext>
        <local:MainPageViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:GridHeightConverter x:Key="GridHeightConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid
        Padding="10"
        ColumnDefinitions="*,*"
        RowDefinitions="Auto,*">
        <Button
            x:Name="uxButton1"
            Grid.Row="0"
            Grid.Column="0"
            Command="{Binding LoadOneItemCommand}"
            HorizontalOptions="Center"
            Text="1 Item"
            VerticalOptions="Center" />

        <Button
            x:Name="uxButton2"
            Grid.Row="0"
            Grid.Column="1"
            Command="{Binding LoadManyItemsCommand}"
            HorizontalOptions="Center"
            Text="More Items"
            VerticalOptions="Center" />

        <CollectionView
            x:Name="uxCollectionView"
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Margin="10"
            ItemsSource="{Binding Items}"
            VerticalOptions="Fill">
            <CollectionView.ItemsLayout>
                <GridItemsLayout
                    HorizontalItemSpacing="8"
                    Orientation="Vertical"
                    Span="3"
                    VerticalItemSpacing="8" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="LightGray" StrokeShape="RoundRectangle 15">
                        <Grid
                            BackgroundColor="WhiteSmoke"
                            HeightRequest="150"
                            RowDefinitions="*,Auto">
                            <ffimageloading:CachedImage
                                Grid.Row="0"
                                Aspect="AspectFill"
                                Source="dotnet_bot.png" />
                            <Label
                                Grid.Row="1"
                                HorizontalOptions="Center"
                                Text="Test"
                                TextColor="Black"
                                VerticalOptions="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <ActivityIndicator
            Grid.Row="1"
            Grid.ColumnSpan="2"
            HeightRequest="40"
            HorizontalOptions="Center"
            IsRunning="{Binding IsBusy}"
            IsVisible="{Binding IsBusy}"
            VerticalOptions="Center"
            WidthRequest="40" />
    </Grid>

</ContentPage>
